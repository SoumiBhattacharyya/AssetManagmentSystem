import { FilterAssetsPipe } from './filter-assets.pipe';

describe('FilterAssetsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAssetsPipe();
    expect(pipe).toBeTruthy();
  });
});
